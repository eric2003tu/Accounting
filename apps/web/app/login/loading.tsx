import BrandLoadingScreen from '@/app/components/BrandLoadingScreen';

export default function Loading() {
  return <BrandLoadingScreen title="Loading sign in" subtitle="Preparing the authentication experience." />;
}