import BrandLoadingScreen from '@/app/components/BrandLoadingScreen';

export default function Loading() {
  return <BrandLoadingScreen title="Loading admin console" subtitle="Preparing platform controls, records, and security views." />;
}