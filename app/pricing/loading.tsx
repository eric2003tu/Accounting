import BrandLoadingScreen from '@/app/components/BrandLoadingScreen';

export default function Loading() {
  return <BrandLoadingScreen title="Loading pricing" subtitle="Fetching plan details and subscription options." />;
}