import BrandLoadingScreen from '@/app/components/BrandLoadingScreen';

export default function Loading() {
  return <BrandLoadingScreen title="Loading registration" subtitle="Preparing the account setup flow." />;
}