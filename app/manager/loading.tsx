import BrandLoadingScreen from '@/app/components/BrandLoadingScreen';

export default function Loading() {
  return <BrandLoadingScreen title="Loading manager workspace" subtitle="Syncing the assigned business and operational reports." />;
}